{
    "version": 2,
    "name": "periodic-api",
    "builds": [{
        "src": "Cargo.toml",
        "use": "@now/rust",
        "config": {
            "rust": "nightly"
        }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/src/main.rs"
    }]
}
